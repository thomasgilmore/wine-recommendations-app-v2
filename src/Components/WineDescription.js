import React, { Component } from 'react';
import './description.css';

export default class WineDescription extends Component {
    render() {
        return (
            <div>
                <p className="description">&emsp;Find a dish that goes well with a given wine (ex. Malbec, Pinot Grigio, Rose Wine, ... etc.).</p>
            </div>
        )
    }
}
